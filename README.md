rm -rf .git (for rinit to init )
...................for push...................................
git init
git add .
git commit -m "commit message"
git remote add origin <github link>
git push -u origin main/master


......................for pull.........................

git clone <github link >
Pwd (for path of Cline folder)
......Open folder......In bash  
Git remote -v 
 git add .
git commit -m 'messge'
git push -u origin main/master

Go to Repo -> create pull riq

